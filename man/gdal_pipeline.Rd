% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdal_pipeline.R, R/gdal_pipeline.R
\name{gdal_pipeline}
\alias{gdal_pipeline}
\title{Process GDAL Pipeline (Convenience Wrapper)}
\usage{
gdal_pipeline(
  input = NULL,
  output = NULL,
  output_format = NULL,
  pipeline = NULL
)

gdal_pipeline(
  input = NULL,
  output = NULL,
  output_format = NULL,
  pipeline = NULL
)
}
\arguments{
\item{input}{Input raster or vector datasets. \code{1} to \code{2147483647} value(s). Can also be a \link{gdal_job} object to extend a pipeline}

\item{output}{Output raster or vector dataset (Dataset path)}

\item{output_format}{Output format ("GDALG" allowed)}

\item{pipeline}{Pipeline string or filename}

\item{jobs}{A list or vector of \code{gdal_job} objects to execute in sequence,
or NULL to use pipeline string}

\item{...}{Additional arguments passed to \code{gdal_raster_pipeline()} or \code{gdal_vector_pipeline()}}
}
\value{
A \code{gdal_job} object representing the pipeline.

A \link{gdal_job} object.
}
\description{
Convenience function that automatically detects whether a pipeline contains
raster or vector operations and delegates to the appropriate \code{gdal_raster_pipeline()}
or \code{gdal_vector_pipeline()} function.

This function is useful when you want a single unified interface to process
pipelines without needing to explicitly choose the raster or vector variant.

Process a dataset applying several steps.

See \url{https://gdal.org/en/3.12.0/programs/gdal_pipeline.html} for detailed GDAL documentation.
}
\details{
The pipeline type is determined by examining the first job's command_path:
\itemize{
\item If the first job is a raster command, \code{gdal_raster_pipeline()} is used
\item If the first job is a vector command, \code{gdal_vector_pipeline()} is used
\item Default is raster if type cannot be determined
}
}
\examples{
\dontrun{
# Auto-detect based on job type
job1 <- gdal_raster_reproject(input = "input.tif", dst_crs = "EPSG:32632")
job2 <- gdal_raster_convert(output = "output.tif")

# This will automatically use gdal_raster_pipeline
pipeline <- gdal_pipeline(jobs = list(job1, job2))
}

\dontrun{
# TODO: No examples available for gdal_pipeline.
# See GDAL documentation: https://gdal.org/programs/gdal-pipeline.html
job <- gdal_pipeline()
# gdal_job_run(job)
}
}
\concept{gdal_pipeline_utilities}
