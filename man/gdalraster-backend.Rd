% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topics-gdalraster-backend.R
\name{gdalraster-backend}
\alias{gdalraster-backend}
\title{gdalraster Backend - C++ Execution Acceleration}
\description{
The gdalraster backend provides fast C++ execution through Rcpp bindings,
eliminating subprocess overhead. This is the fastest execution path for
batch GDAL operations.

gdalcli automatically selects the best available backend:
\enumerate{
\item gdalraster (if installed, gdalraster >= 2.2.0)
\item processx (system GDAL subprocess, fallback)
\item reticulate (Python GDAL bindings, if configured)
}
}
\details{
\subsection{Why Use gdalraster?}{

\strong{Advantages:}
\itemize{
\item Fast C++ execution via Rcpp bindings (no subprocess overhead)
\item Works on Windows with binary package installation
\item Native R integration for data structures (sf objects, matrices, etc.)
\item In-memory vector processing with GDAL 3.12+ (gdalraster 2.3.0+)
\item Better debugging and error messages from C++ layer
}

\strong{When to use:}
\itemize{
\item Windows systems without system GDAL installed
\item Performance-critical applications requiring repeated GDAL operations
\item In-memory vector processing workflows
\item When you prefer pure R dependencies without system requirements
}
}

\subsection{Actual Performance (Benchmarked)}{

\strong{Test Setup:} 50 command discovery operations with gdalraster 2.2.1 + GDAL 3.11.4

\strong{Results:}
\itemize{
\item gdalraster (Rcpp): 0.145 seconds (2.9 ms per call)
\item processx (subprocess): 2.597 seconds (51.94 ms per call)
\item \strong{Speedup: 17.91x}
}

\strong{Projected savings at scale:}
\itemize{
\item 100 operations: ~4.9 seconds saved
\item 500 operations: ~24 seconds saved
\item 1000 operations: ~49 seconds saved
}

\strong{Key finding:} Subprocess startup overhead is ~49 ms per operation.
}

\subsection{Installation}{
\subsection{Windows}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{install.packages("gdalraster")  # Pre-compiled binaries, no system GDAL needed
}\if{html}{\out{</div>}}
}

\subsection{macOS}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{install.packages("gdalraster")
}\if{html}{\out{</div>}}

If binary installation fails, install system GDAL first:

\if{html}{\out{<div class="sourceCode bash">}}\preformatted{brew install gdal
}\if{html}{\out{</div>}}
}

\subsection{Linux}{

Install system GDAL first:

\if{html}{\out{<div class="sourceCode bash">}}\preformatted{# Ubuntu/Debian
sudo apt-get install gdal-bin libgdal-dev

# Fedora/RHEL
sudo dnf install gdal gdal-devel

# Arch
sudo pacman -S gdal
}\if{html}{\out{</div>}}

Then install the R package:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{install.packages("gdalraster")
}\if{html}{\out{</div>}}
}

}

\subsection{Feature Availability by Version}{\tabular{llll}{
   Feature \tab gdalraster 2.2.0+ \tab gdalraster 2.3.0+ \tab Notes \cr
   Job execution via gdal_alg() \tab ✓ Yes \tab ✓ Yes \tab Core execution engine \cr
   Command discovery \tab ✓ Yes \tab ✓ Yes \tab List available operations \cr
   Command help \tab ✓ Yes \tab ✓ Yes \tab Get help text \cr
   Explicit argument access \tab ✓ Yes \tab ✓ Yes \tab For audit logging \cr
   Vector from object (in-memory) \tab ✗ No \tab ✓ Yes \tab Process R objects without temp files \cr
   Advanced features \tab ✗ No \tab ✓ Yes \tab Extended functionality \cr
}


\strong{Minimum requirements:}
\itemize{
\item gdalraster >= 2.2.0 for basic execution
\item gdalraster >= 2.3.0 for advanced vector processing
\item GDAL >= 3.11 for unified CLI commands
}
}
}
\examples{
\dontrun{
  library(gdalcli)

  # Auto-selection: gdalraster if available, otherwise processx
  job <- gdal_raster_info("test.tif")
  result <- gdal_job_run(job)

  # Force specific backend
  result <- gdal_job_run(job, backend = "gdalraster")

  # Check gdalraster version
  gdalcli:::.get_gdalraster_version()

  # Check if feature is available
  gdalcli:::.gdal_has_feature("setVectorArgsFromObject")
}

}
\seealso{
\code{\link[=gdal_job_run]{gdal_job_run()}} for backend selection,
\link{backends} for all backend options,
\link{GDAL-features} for GDAL 3.12+ features
}
\keyword{internal}
