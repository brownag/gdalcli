% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdal-object.R
\name{gdal.alg}
\alias{gdal.alg}
\title{Dynamic GDAL API Object}
\format{
An \link[R6:R6Class]{R6::R6Class} object with reference semantics.
}
\usage{
gdal.alg()
}
\description{
The \code{gdal.alg} object is the main entry point for the dynamic GDAL API.
It provides hierarchical access to all GDAL commands via R6-based
nested objects.
}
\details{
The \code{gdal.alg} object is created automatically when gdalcli is loaded
(requires GDAL >= 3.11 and gdalraster package). It mirrors the structure
of Python's \code{gdal.alg} module.
\subsection{Usage}{

Access commands using the \code{$} operator at multiple levels:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Top level: access command groups
gdal.alg$raster
gdal.alg$vector
gdal.alg$mdim

# Second level: access specific commands
gdal.alg$raster$info
gdal.alg$raster$convert
gdal.alg$vector$translate

# Call commands to create lazy-evaluated jobs
job <- gdal.alg$raster$info(input = "data.tif")
result <- gdal_run(job)
}\if{html}{\out{</div>}}
}

\subsection{Command Groups}{

Available command groups depend on your GDAL installation:
\itemize{
\item \strong{raster}: Raster file operations (info, convert, translate, etc.)
\item \strong{vector}: Vector file operations (convert, translate, info, etc.)
\item \strong{mdim}: Multidimensional data operations
\item \strong{vsi}: Virtual File System operations
\item \strong{driver}: Driver-specific utilities
}
}

\subsection{Lazy Evaluation}{

All commands return \code{gdal_job} objects that are executed lazily.
Compose jobs using pipe (\verb{|>}) and helper functions:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{gdal.alg$raster$convert(input = "in.tif", output = "out.tif") |>
  gdal_with_co("COMPRESS=LZW") |>
  gdal_with_config("GDAL_NUM_THREADS=4") |>
  gdal_run()
}\if{html}{\out{</div>}}
}

\subsection{IDE Autocompletion}{

The \code{gdal.alg} object supports full IDE autocompletion in:
\itemize{
\item RStudio
\item VSCode with R extension
\item Emacs with ESS
}

Type \verb{gdal.alg$} and press Tab to see available groups,
then type \verb{gdal.alg$raster$} and press Tab for available commands.
}
}
\examples{
\dontrun{
  # Check available command groups
  gdal.alg

  # Create a raster info job
  job <- gdal.alg()$raster$info(input = "data.tif")

  # Execute with gdal_run
  result <- gdal_run(job)
}

}
\seealso{
\code{\link[=gdal_run]{gdal_run()}}, \code{\link[=gdal_with_co]{gdal_with_co()}}, \code{\link[=gdal_with_config]{gdal_with_config()}},
\code{\link[=new_gdal_job]{new_gdal_job()}}
}
