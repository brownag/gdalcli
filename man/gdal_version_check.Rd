% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdal-version-check.R
\name{gdal_version_check}
\alias{gdal_version_check}
\title{Check GDAL Version Compatibility}
\usage{
gdal_version_check()
}
\value{
A list with elements:
\itemize{
\item \code{package_gdal_version}: GDAL version the package was built for
\item \code{runtime_gdal_version}: Currently installed GDAL version
\item \code{compatible}: Logical indicating if versions match
\item \code{message}: Human-readable status message
}
}
\description{
Verifies that the runtime GDAL version matches (or is compatible with)
the GDAL version used to build the gdalcli package.

This function is useful when using version-pinned release branches, where
specific GDAL versions are pre-computed for optimal compatibility.
}
\details{
Compatibility is determined by comparing major.minor version numbers.
For example, GDAL 3.11.0 and 3.11.5 are compatible, but 3.11.x and
3.12.x are not.
}
\examples{
\dontrun{
  result <- gdal_version_check()
  cat(result$message, "\n")

  if (!result$compatible) {
    warning("Consider switching to a different release branch")
  }
}

}
\seealso{
\code{\link[=gdal_job_run]{gdal_job_run()}}, \code{\link[gdalraster:gdal_version]{gdalraster::gdal_version()}}
}
