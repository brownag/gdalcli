% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-explicit-args.R
\name{gdal_job_get_explicit_args}
\alias{gdal_job_get_explicit_args}
\title{Extract Explicitly Set Arguments from GDAL Job}
\usage{
gdal_job_get_explicit_args(job, system_only = FALSE)
}
\arguments{
\item{job}{A \code{gdal_job} object to inspect}

\item{system_only}{Logical. If TRUE, filters results to show only
system-level arguments (e.g., "-q", "-quiet"). Default FALSE returns
all explicitly set args.}
}
\value{
Named list of explicitly set arguments and their values.
Returns empty list if the feature is unavailable or no explicit args
were set.
}
\description{
Retrieves the list of arguments that were explicitly set by the user
(versus those using system defaults) for a GDAL job.

This function uses gdalraster's GDALAlg class \code{getExplicitlySetArgs()}
method, which is available with GDAL 3.12+ (gdalraster 2.2.0+).
}
\details{
The distinction between explicitly set and default arguments is valuable for:

\enumerate{
\item \strong{Audit Logging}: Recording what the user actually specified
\item \strong{Configuration Introspection}: Understanding configuration state
\item \strong{Reproducibility}: Saving configuration for later replay
\item \strong{Debugging}: Identifying unintended defaults
}

This capability requires GDAL 3.12+ because earlier versions of the GDAL
Options API do not track this distinction.
}
\section{Performance}{

The function caches feature availability to avoid repeated version checks.
First call may be slightly slower; subsequent calls use cached result.
}

\section{Graceful Degradation}{

If GDAL < 3.12 or gdalraster binding unavailable:
\itemize{
\item Returns empty character vector
\item Issues informational warning
\item Continues execution normally
}
}

\examples{
\dontrun{
  # Create a job with explicit arguments
  job <- new_gdal_job(
    command_path = c("raster", "convert"),
    arguments = list(
      input = "input.tif",
      output = "output.tif",
      output_format = "COG",
      creation_option = c("COMPRESS=LZW")
    )
  )

  # Get all explicitly set arguments (returns named list)
  explicit_args <- gdal_job_get_explicit_args(job)
  # Result is a named list like:
  # $output_format
  # [1] "COG"
  # $creation_option
  # [1] "COMPRESS=LZW"

  # Get only system-level arguments
  system_args <- gdal_job_get_explicit_args(job, system_only = TRUE)
}

}
\seealso{
\code{\link[=gdal_job_run_with_audit]{gdal_job_run_with_audit()}} for using explicit args in audit logging,
\code{\link[=gdal_capabilities]{gdal_capabilities()}} to check feature availability
}
