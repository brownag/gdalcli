% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-gdalcli_options.R
\name{gdalcli_options}
\alias{gdalcli_options}
\title{Manage gdalcli Options}
\usage{
gdalcli_options(...)
}
\arguments{
\item{...}{Named arguments to set options. Option names should be provided
without the "gdalcli." prefix (e.g., \code{backend = "processx"} not
\code{gdalcli.backend = "processx"}). If no arguments are provided, returns
a list of all option values.}
}
\value{
When called without arguments, returns a named list of all option values.
When called with arguments, invisibly returns the previous values as a list.
}
\description{
Get and set package options for gdalcli. Options control default behaviors
for execution, output, and logging.
}
\details{
\subsection{Available Options}{
\itemize{
\item \code{backend}: Which backend to use for command execution. Options are:
\itemize{
\item \code{"auto"} (default): Automatically select based on available backends
\item \code{"gdalraster"}: Use gdalraster backend (faster, direct API calls)
\item \code{"processx"}: Use processx backend (universal, subprocess-based)
\item \code{"reticulate"}: Use reticulate backend (Python GDAL bindings)
}
\item \code{stream_out_format}: Format for streaming output. Options are:
\itemize{
\item \code{NULL} (default): No streaming format preference
\item \code{"text"}: Use text format for streaming output
\item \code{"binary"}: Use binary format for streaming output
}
\item \code{verbose}: Enable verbose output during job execution.
\itemize{
\item \code{FALSE} (default): No verbose output
\item \code{TRUE}: Print detailed execution information
}
\item \code{audit_logging}: Enable audit logging of executed commands.
\itemize{
\item \code{FALSE} (default): No audit logging
\item \code{TRUE}: Log all commands to audit trail (requires setting up audit handler)
}
}
}
}
\examples{
# Get current option values
gdalcli_options()

# Set options
gdalcli_options(backend = "processx", verbose = TRUE)

# Set multiple options at once
gdalcli_options(
  backend = "gdalraster",
  verbose = FALSE,
  audit_logging = TRUE
)

}
