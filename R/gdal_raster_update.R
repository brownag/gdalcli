# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.2
# ===================================================================

#' @title update: Update the destination raster with the content of the input one
#' @description
#' `gdal raster update` can be used to update an existing output raster dataset with the pixel values of the input raster, doing reprojection if the input and output datasets do not have the same coordinate reference systems. The extent, size, resolution or coordinate reference system of the output dataset are not modified by this operation. Overviews are updated by this command (using gdal_raster_overview_refresh), unless --no-update-overviews is specified. Starting with GDAL 3.13, gdal raster update can be used as a step of a pipeline, with the input dataset being the output of the previous step.
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_raster_update.html} for detailed GDAL documentation.
#' @param input Input raster datasets (required). Exactly `1` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param input_format Input formats (Character vector) (Advanced)
#' @param output Output raster dataset (Dataset path) (required)
#' @param quiet Quiet mode (no progress bar or warning message) (Logical) (Common)
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>` (Advanced)
#' @param geometry Clipping geometry (WKT or GeoJSON)
#' @param geometry_crs CRS of clipping geometry
#' @param resampling Resampling method. Choices: "nearest", "bilinear", "cubic", "cubicspline", "lanczos", ... (Default: `nearest`)
#' @param warp_option Warping option(s) (Character vector). Format: `<NAME>=<VALUE>` (Advanced)
#' @param transform_option Transform option(s) (Character vector). Format: `<NAME>=<VALUE>` (Advanced)
#' @param error_threshold Error threshold. Minimum: `0` (Advanced)
#' @param no_update_overviews Do not update existing overviews (Logical)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_raster_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_raster_update.
#' # See GDAL documentation at:
#' https://gdal.org/en/release-3.13/programs/gdal_raster_update.html
#' job <- gdal_raster_update()
#' # gdal_job_run(job)
#' }
#' @export
gdal_raster_update <- function(input,
  input_format = NULL,
  output,
  quiet = FALSE,
  open_option = NULL,
  geometry = NULL,
  geometry_crs = NULL,
  resampling = NULL,
  warp_option = NULL,
  transform_option = NULL,
  error_threshold = NULL,
  no_update_overviews = FALSE,
  ...) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(output)) new_args[["output"]] <- output
  if (!missing(quiet)) new_args[["quiet"]] <- quiet
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(geometry)) new_args[["geometry"]] <- geometry
  if (!missing(geometry_crs)) new_args[["geometry_crs"]] <- geometry_crs
  if (!missing(resampling)) new_args[["resampling"]] <- resampling
  if (!missing(warp_option)) new_args[["warp_option"]] <- warp_option
  if (!missing(transform_option)) new_args[["transform_option"]] <- transform_option
  if (!missing(error_threshold)) new_args[["error_threshold"]] <- error_threshold
  if (!missing(no_update_overviews)) new_args[["no_update_overviews"]] <- no_update_overviews


  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("raster", "update"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_raster_update", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    input = list(min_count = 1, max_count = 1),
    input_format = list(min_count = 0, max_count = 2147483647),
    output = list(min_count = 0, max_count = 1),
    quiet = list(min_count = 0, max_count = 1),
    open_option = list(min_count = 0, max_count = 2147483647),
    geometry = list(min_count = 0, max_count = 1),
    geometry_crs = list(min_count = 0, max_count = 1),
    resampling = list(min_count = 0, max_count = 1),
    warp_option = list(min_count = 0, max_count = 2147483647),
    transform_option = list(min_count = 0, max_count = 2147483647),
    error_threshold = list(min_count = 0, max_count = 1),
    no_update_overviews = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("raster", "update"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

