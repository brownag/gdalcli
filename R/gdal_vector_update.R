# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.0
# ===================================================================

#' @title update: Update an existing vector dataset with an input vector dataset
#' @description
#' `gdal vector update` can be used to update the features of an existing output vector dataset with the features of the input vector. It identifies matching features between input and output based on their feature ID by default, or based on the value of one or several columns forming a key, when using --key. With the default ``merge`` mode, features found in both layers will be updated, and features only existing in the input layer will be created in the output layer. The schemas of the input and output layers do not need to be identical (except on columns specified by --key), but they should use the same CRS for geometry columns, as no on-the-fly reprojection is done. If a column exists in the input layer but not in the output layer, it will not be created. And if a column exists in the output layer and not in the input layer, its content will not be updated. gdal vector update can be used as a step of a pipeline, with the input dataset being the output of the previous step.
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_vector_update.html} for detailed GDAL documentation.
#' @param input Input vector datasets (required). Exactly `1` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param input_format Input formats (Character vector) (Advanced)
#' @param input_layer Input layer name (Character vector). `0` to `1` value(s)
#' @param output Output vector dataset (Dataset path) (required)
#' @param output_open_option Output open options (Character vector). Format: `<KEY>=<VALUE>` (Advanced)
#' @param output_layer Output layer name
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>` (Advanced)
#' @param quiet Quiet mode (no progress bar or warning message) (Logical) (Common)
#' @param mode Set update mode. Choices: "merge", "update-only", "append-only" (Default: `merge`)
#' @param key Field(s) used as a key to identify features (Character vector)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_vector_utilities
#' @examples

#' job <- gdal_vector_update(input = "in.gpkg", output = "out.gpkg", key = "identifier")
#' \dontrun{
#'   result <- gdal_job_run(job)
#' }
#' @export
gdal_vector_update <- function(input,
  input_format = NULL,
  input_layer = NULL,
  output,
  output_open_option = NULL,
  output_layer = NULL,
  open_option = NULL,
  quiet = FALSE,
  mode = NULL,
  key = NULL,
  ...) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(input_layer)) new_args[["input_layer"]] <- input_layer
  if (!missing(output)) new_args[["output"]] <- output
  if (!missing(output_open_option)) new_args[["output_open_option"]] <- output_open_option
  if (!missing(output_layer)) new_args[["output_layer"]] <- output_layer
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(quiet)) new_args[["quiet"]] <- quiet
  if (!missing(mode)) new_args[["mode"]] <- mode
  if (!missing(key)) new_args[["key"]] <- key


  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("vector", "update"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_vector_update", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    input = list(min_count = 1, max_count = 1),
    input_format = list(min_count = 0, max_count = 2147483647),
    input_layer = list(min_count = 0, max_count = 1),
    output = list(min_count = 0, max_count = 1),
    output_open_option = list(min_count = 0, max_count = 2147483647),
    output_layer = list(min_count = 0, max_count = 1),
    open_option = list(min_count = 0, max_count = 2147483647),
    quiet = list(min_count = 0, max_count = 1),
    mode = list(min_count = 0, max_count = 1),
    key = list(min_count = 0, max_count = 2147483647)
  )

  new_gdal_job(command_path = c("vector", "update"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

