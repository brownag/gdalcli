# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.12.2
# Generation date: 2026-02-13
# ===================================================================

#' @title info: Return information on a raster dataset
#' @description
#' Return information on a raster dataset.
#' 
#' See \url{https://gdal.org/en/3.12.2/programs/gdal_raster_info.html} for detailed GDAL documentation.
#' @param input Input raster dataset. Format: `NA`. Choices: NULL (required). Minimum: `NA`. Exactly `1` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param input_format Input formats (Character vector). Format: `NA`. Choices: NULL. Minimum: `NA`. `0` to `2147483647` value(s) (Advanced)
#' @param output_format Output format. Format: `NA`. Choices: c("json", "text"). Minimum: `NA`
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>`. Choices: NULL. Minimum: `NA`. `0` to `2147483647` value(s) (Advanced)
#' @param min_max Compute minimum and maximum value (Logical). Format: `NA`. Choices: NULL. Minimum: `NA`
#' @param stats Retrieve or compute statistics, using all pixels (Logical). Format: `NA`. Choices: NULL. Minimum: `NA`
#' @param approx_stats Retrieve or compute statistics, using a subset of pixels (Logical). Format: `NA`. Choices: NULL. Minimum: `NA`
#' @param hist Retrieve or compute histogram (Logical). Format: `NA`. Choices: NULL. Minimum: `NA`
#' @param no_gcp Suppress ground control points list printing (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param no_md Suppress metadata printing (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param no_ct Suppress color table printing (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param no_fl Suppress file list printing (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param checksum Compute pixel checksum (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param list_mdd List all metadata domains available for the dataset (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param metadata_domain Report metadata for the specified domain. 'all' can be used to report metadata in all domains. Format: `NA`. Choices: NULL. Minimum: `NA` (Advanced)
#' @param no_nodata Suppress retrieving nodata value (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Esoteric)
#' @param no_mask Suppress mask band information (Logical). Format: `NA`. Choices: NULL. Minimum: `NA` (Esoteric)
#' @param subdataset Use subdataset of specified index (starting at 1), instead of the source dataset itself (Integer). Format: `NA`. Choices: NULL. Minimum: `1` (Esoteric)
#' @return A [gdal_job] object.
#' @family gdal_raster_utilities
#' @examples

#' job <- gdal_raster_info(input = "utmsmall.tif")
#' \dontrun{
#'   result <- gdal_job_run(job)
#' }
#' @export
gdal_raster_info <- function(input,
  input_format = NULL,
  output_format = NULL,
  open_option = NULL,
  min_max = FALSE,
  stats = FALSE,
  approx_stats = FALSE,
  hist = FALSE,
  no_gcp = FALSE,
  no_md = FALSE,
  no_ct = FALSE,
  no_fl = FALSE,
  checksum = FALSE,
  list_mdd = FALSE,
  metadata_domain = NULL,
  no_nodata = FALSE,
  no_mask = FALSE,
  subdataset = NULL) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(output_format)) new_args[["output_format"]] <- output_format
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(min_max)) new_args[["min_max"]] <- min_max
  if (!missing(stats)) new_args[["stats"]] <- stats
  if (!missing(approx_stats)) new_args[["approx_stats"]] <- approx_stats
  if (!missing(hist)) new_args[["hist"]] <- hist
  if (!missing(no_gcp)) new_args[["no_gcp"]] <- no_gcp
  if (!missing(no_md)) new_args[["no_md"]] <- no_md
  if (!missing(no_ct)) new_args[["no_ct"]] <- no_ct
  if (!missing(no_fl)) new_args[["no_fl"]] <- no_fl
  if (!missing(checksum)) new_args[["checksum"]] <- checksum
  if (!missing(list_mdd)) new_args[["list_mdd"]] <- list_mdd
  if (!missing(metadata_domain)) new_args[["metadata_domain"]] <- metadata_domain
  if (!missing(no_nodata)) new_args[["no_nodata"]] <- no_nodata
  if (!missing(no_mask)) new_args[["no_mask"]] <- no_mask
  if (!missing(subdataset)) new_args[["subdataset"]] <- subdataset

  # Check if first argument is a piped gdal_job or actual data
  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("raster", "info"), new_args))
  }

  # First argument is actual data or missing - create new job
  merged_args <- new_args

  .arg_mapping <- list(
    input = list(min_count = 1, max_count = 1),
    input_format = list(min_count = 0, max_count = 2147483647),
    output_format = list(min_count = NA, max_count = NA),
    open_option = list(min_count = 0, max_count = 2147483647),
    min_max = list(min_count = NA, max_count = NA),
    stats = list(min_count = NA, max_count = NA),
    approx_stats = list(min_count = NA, max_count = NA),
    hist = list(min_count = NA, max_count = NA),
    no_gcp = list(min_count = NA, max_count = NA),
    no_md = list(min_count = NA, max_count = NA),
    no_ct = list(min_count = NA, max_count = NA),
    no_fl = list(min_count = NA, max_count = NA),
    checksum = list(min_count = NA, max_count = NA),
    list_mdd = list(min_count = NA, max_count = NA),
    metadata_domain = list(min_count = NA, max_count = NA),
    no_nodata = list(min_count = NA, max_count = NA),
    no_mask = list(min_count = NA, max_count = NA),
    subdataset = list(min_count = NA, max_count = NA)
  )

  new_gdal_job(command_path = c("raster", "info"), arguments = merged_args, arg_mapping = .arg_mapping)
}

