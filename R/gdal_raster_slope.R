# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.11.4
# Generation date: 2026-02-13
# ===================================================================

#' @title slope: Generate a slope map
#' @description
#' Generate a slope map
#' 
#' See \url{https://gdal.org/en/3.11.4/programs/gdal_raster_slope.html} for detailed GDAL documentation.
#' @param input Input raster dataset (Dataset path). Format: `NA`. Choices: NULL (Default: `NULL`) (required). Minimum: `NA`. Can also be a [gdal_job] object to extend a pipeline
#' @param input_format Input formats (Character vector). Format: `NA`. Choices: NULL (Default: `NULL`). Minimum: `NA`. `0` to `2147483647` value(s) (Advanced)
#' @param output Output raster dataset (Dataset path) (required)
#' @param output_format Output format ("GDALG" allowed). Format: `NA`. Choices: NULL (Default: `NULL`). Minimum: `NA`
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>`. Choices: NULL (Default: `NULL`). Minimum: `NA`. `0` to `2147483647` value(s) (Advanced)
#' @param creation_option Creation option (Character vector). Format: `<KEY>=<VALUE>`. Choices: NULL (Default: `NULL`). Minimum: `NA`. `0` to `2147483647` value(s)
#' @param overwrite Whether overwriting existing output is allowed (Logical). Format: `NA`. Choices: NULL (Default: `FALSE`). Minimum: `NA`
#' @param band Input band (1-based index) (Integer). Format: `NA`. Choices: NULL (Default: `1`). Minimum: `NA`
#' @param unit Unit in which to express slopes. Format: `NA`. Choices: c("degree", "percent") (Default: `degree`). Minimum: `NA`
#' @param xscale Ratio of vertical units to horizontal X axis units. Format: `NA`. Choices: NULL (Default: `NULL`). Minimum: `0`
#' @param yscale Ratio of vertical units to horizontal Y axis units. Format: `NA`. Choices: NULL (Default: `NULL`). Minimum: `0`
#' @param gradient_alg Algorithm used to compute terrain gradient. Format: `NA`. Choices: c("Horn", "ZevenbergenThorne") (Default: `Horn`). Minimum: `NA`
#' @param no_edges Do not try to interpolate values at dataset edges or close to nodata values (Logical). Format: `NA`. Choices: NULL (Default: `NULL`). Minimum: `NA`
#' @return A [gdal_job] object.
#' @family gdal_raster_utilities
#' @examples

#' job <- gdal_raster_slope(input = "n43.dt0", output = "out.tif", overwrite = TRUE)
#' \dontrun{
#'   result <- gdal_job_run(job)
#' }
#' @export
gdal_raster_slope <- function(input,
  input_format = NULL,
  output,
  output_format = NULL,
  open_option = NULL,
  creation_option = NULL,
  overwrite = FALSE,
  band = NULL,
  unit = NULL,
  xscale = NULL,
  yscale = NULL,
  gradient_alg = NULL,
  no_edges = FALSE) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(output)) new_args[["output"]] <- output
  if (!missing(output_format)) new_args[["output_format"]] <- output_format
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(creation_option)) new_args[["creation_option"]] <- creation_option
  if (!missing(overwrite)) new_args[["overwrite"]] <- overwrite
  if (!missing(band)) new_args[["band"]] <- band
  if (!missing(unit)) new_args[["unit"]] <- unit
  if (!missing(xscale)) new_args[["xscale"]] <- xscale
  if (!missing(yscale)) new_args[["yscale"]] <- yscale
  if (!missing(gradient_alg)) new_args[["gradient_alg"]] <- gradient_alg
  if (!missing(no_edges)) new_args[["no_edges"]] <- no_edges

  # Check if first argument is a piped gdal_job or actual data
  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("raster", "slope"), new_args))
  }

  # First argument is actual data or missing - create new job
  merged_args <- new_args

  .arg_mapping <- list(
    input = list(min_count = NA, max_count = NA),
    input_format = list(min_count = 0, max_count = 2147483647),
    output = list(min_count = 0, max_count = 1),
    output_format = list(min_count = NA, max_count = NA),
    open_option = list(min_count = 0, max_count = 2147483647),
    creation_option = list(min_count = 0, max_count = 2147483647),
    overwrite = list(min_count = NA, max_count = NA),
    band = list(min_count = NA, max_count = NA),
    unit = list(min_count = NA, max_count = NA),
    xscale = list(min_count = NA, max_count = NA),
    yscale = list(min_count = NA, max_count = NA),
    gradient_alg = list(min_count = NA, max_count = NA),
    no_edges = list(min_count = NA, max_count = NA)
  )

  new_gdal_job(command_path = c("raster", "slope"), arguments = merged_args, arg_mapping = .arg_mapping)
}

