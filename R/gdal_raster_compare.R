# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.1
# ===================================================================

#' @title compare: Compare two raster datasets
#' @description
#' `gdal raster compare` compares two GDAL supported datasets and reports the differences. In addition to reporting differences to the standard output, the program will also return the difference count in its exit value. As a convention, the first dataset specified as a positional argument, or through --reference, is assumed to be the reference/exact/golden dataset. The second dataset specified as a positional argument, or through --input, is the dataset compared to the reference dataset. Image pixels, and various metadata are checked. There is also a byte by byte comparison done which will count as one difference. So, if it is only important that the GDAL visible data is identical, a difference count of 1 (the binary difference) should be considered acceptable, or you may specify --skip-binary to omit byte to byte comparison. This program can also be used as the last step of a raster pipeline <gdal_raster_pipeline>. The following options are available:
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_raster_compare.html} for detailed GDAL documentation.
#' @param input Input raster dataset (required). Exactly `1` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param reference Reference dataset (Dataset path) (required)
#' @param quiet Quiet mode (no progress bar or warning message) (Logical) (Common)
#' @param input_format Input formats (Character vector) (Advanced)
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>` (Advanced)
#' @param skip_all_optional Skip all optional comparisons (Logical)
#' @param skip_binary Skip binary file comparison (Logical)
#' @param skip_crs Skip CRS comparison (Logical)
#' @param skip_geotransform Skip geotransform comparison (Logical)
#' @param skip_overview Skip overview comparison (Logical)
#' @param skip_metadata Skip metadata comparison (Logical)
#' @param skip_rpc Skip RPC metadata comparison (Logical)
#' @param skip_geolocation Skip Geolocation metadata comparison (Logical)
#' @param skip_subdataset Skip subdataset comparison (Logical)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_raster_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_raster_compare.
#' # See GDAL documentation at:
#' https://gdal.org/en/release-3.13/programs/gdal_raster_compare.html
#' job <- gdal_raster_compare()
#' # gdal_job_run(job)
#' }
#' @export
gdal_raster_compare <- function(input,
  reference,
  quiet = FALSE,
  input_format = NULL,
  open_option = NULL,
  skip_all_optional = FALSE,
  skip_binary = FALSE,
  skip_crs = FALSE,
  skip_geotransform = FALSE,
  skip_overview = FALSE,
  skip_metadata = FALSE,
  skip_rpc = FALSE,
  skip_geolocation = FALSE,
  skip_subdataset = FALSE,
  ...) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(reference)) new_args[["reference"]] <- reference
  if (!missing(quiet)) new_args[["quiet"]] <- quiet
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(skip_all_optional)) new_args[["skip_all_optional"]] <- skip_all_optional
  if (!missing(skip_binary)) new_args[["skip_binary"]] <- skip_binary
  if (!missing(skip_crs)) new_args[["skip_crs"]] <- skip_crs
  if (!missing(skip_geotransform)) new_args[["skip_geotransform"]] <- skip_geotransform
  if (!missing(skip_overview)) new_args[["skip_overview"]] <- skip_overview
  if (!missing(skip_metadata)) new_args[["skip_metadata"]] <- skip_metadata
  if (!missing(skip_rpc)) new_args[["skip_rpc"]] <- skip_rpc
  if (!missing(skip_geolocation)) new_args[["skip_geolocation"]] <- skip_geolocation
  if (!missing(skip_subdataset)) new_args[["skip_subdataset"]] <- skip_subdataset


  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("raster", "compare"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_raster_compare", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    input = list(min_count = 1, max_count = 1),
    reference = list(min_count = 0, max_count = 1),
    quiet = list(min_count = 0, max_count = 1),
    input_format = list(min_count = 0, max_count = 2147483647),
    open_option = list(min_count = 0, max_count = 2147483647),
    skip_all_optional = list(min_count = 0, max_count = 1),
    skip_binary = list(min_count = 0, max_count = 1),
    skip_crs = list(min_count = 0, max_count = 1),
    skip_geotransform = list(min_count = 0, max_count = 1),
    skip_overview = list(min_count = 0, max_count = 1),
    skip_metadata = list(min_count = 0, max_count = 1),
    skip_rpc = list(min_count = 0, max_count = 1),
    skip_geolocation = list(min_count = 0, max_count = 1),
    skip_subdataset = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("raster", "compare"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

