# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.12.0
# Generation date: 2025-12-14
# ===================================================================

#' @title pansharpen: Perform a pansharpen operation
#' @description
#' Perform a pansharpen operation.
#' 
#' See \url{https://gdal.org/en/3.12.0/programs/gdal_raster_pansharpen.html} for detailed GDAL documentation.
#' @param input Input panchromatic raster dataset (required). Exactly `1` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param input_format Input formats (Character vector). `0` to `2147483647` value(s) (Advanced)
#' @param output Output raster dataset (Dataset path) (required)
#' @param output_format Output format ("GDALG" allowed)
#' @param spectral Input spectral band dataset (required). `1` to `2147483647` value(s)
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>`. `0` to `2147483647` value(s) (Advanced)
#' @param creation_option Creation option (Character vector). Format: `<KEY>=<VALUE>`. `0` to `2147483647` value(s)
#' @param overwrite Whether overwriting existing output is allowed (Logical) (Default: `false`)
#' @param append Append as a subdataset to existing output (Logical) (Default: `false`)
#' @param resampling Resampling algorithm. Choices: nearest, bilinear, cubic, cubicspline, lanczos, ... (Default: `cubic`)
#' @param weights Weight for each input spectral band. `0` to `2147483647` value(s)
#' @param nodata Override nodata value of input bands
#' @param bit_depth Override bit depth of input bands (Integer). Minimum: `8`
#' @param spatial_extent_adjustment Select behavior when bands have not the same extent. Choices: union, intersection, none, none-without-warning (Default: `union`)
#' @param num_threads Number of jobs (or ALL_CPUS) (Default: `ALL_CPUS`)
#' @return A [gdal_job] object.
#' @family gdal_raster_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_raster_pansharpen.
#' # See GDAL documentation: https://gdal.org/programs/gdal-raster-pansharpen.html
#' job <- gdal_raster_pansharpen()
#' # gdal_job_run(job)
#' }
#' @export
gdal_raster_pansharpen <- function(input,
  input_format = NULL,
  output,
  output_format = NULL,
  spectral,
  open_option = NULL,
  creation_option = NULL,
  overwrite = FALSE,
  append = FALSE,
  resampling = NULL,
  weights = NULL,
  nodata = NULL,
  bit_depth = NULL,
  spatial_extent_adjustment = NULL,
  num_threads = NULL) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(output)) new_args[["output"]] <- output
  if (!missing(output_format)) new_args[["output_format"]] <- output_format
  if (!missing(spectral)) new_args[["spectral"]] <- spectral
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(creation_option)) new_args[["creation_option"]] <- creation_option
  if (!missing(overwrite)) new_args[["overwrite"]] <- overwrite
  if (!missing(append)) new_args[["append"]] <- append
  if (!missing(resampling)) new_args[["resampling"]] <- resampling
  if (!missing(weights)) new_args[["weights"]] <- weights
  if (!missing(nodata)) new_args[["nodata"]] <- nodata
  if (!missing(bit_depth)) new_args[["bit_depth"]] <- bit_depth
  if (!missing(spatial_extent_adjustment)) new_args[["spatial_extent_adjustment"]] <- spatial_extent_adjustment
  if (!missing(num_threads)) new_args[["num_threads"]] <- num_threads

  # Check if first argument is a piped gdal_job or actual data
  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("raster", "pansharpen"), new_args))
  }

  # First argument is actual data or missing - create new job
  merged_args <- new_args

  .arg_mapping <- list(
    input = list(min_count = 1, max_count = 1),
    input_format = list(min_count = 0, max_count = 2147483647),
    output = list(min_count = 0, max_count = 1),
    output_format = list(min_count = 0, max_count = 1),
    spectral = list(min_count = 1, max_count = 2147483647),
    open_option = list(min_count = 0, max_count = 2147483647),
    creation_option = list(min_count = 0, max_count = 2147483647),
    overwrite = list(min_count = 0, max_count = 1),
    append = list(min_count = 0, max_count = 1),
    resampling = list(min_count = 0, max_count = 1),
    weights = list(min_count = 0, max_count = 2147483647),
    nodata = list(min_count = 0, max_count = 1),
    bit_depth = list(min_count = 0, max_count = 1),
    spatial_extent_adjustment = list(min_count = 0, max_count = 1),
    num_threads = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("raster", "pansharpen"), arguments = merged_args, arg_mapping = .arg_mapping)
}

