# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.0
# ===================================================================

#' @title identify: Identify driver opening dataset(s)
#' @description
#' Identify driver opening dataset(s).
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_dataset_identify.html} for detailed GDAL documentation.
#' @param filename File or directory name (Character vector) (required). Can also be a [gdal_job] object to extend a pipeline
#' @param output Output vector dataset (Dataset path)
#' @param quiet Quiet mode (no progress bar or warning message) (Logical) (Common)
#' @param output_format Output format
#' @param creation_option Creation option (Character vector). Format: `<KEY>=<VALUE>`
#' @param layer_creation_option Layer creation option (Character vector). Format: `<KEY>=<VALUE>`
#' @param output_layer Output layer name
#' @param overwrite Whether overwriting existing output dataset is allowed (Logical) (Default: `false`)
#' @param recursive Recursively scan files/folders for datasets (Logical)
#' @param force_recursive Recursively scan folders for datasets, forcing recursion in folders recognized as valid formats (Logical)
#' @param detailed Most detailed output. Reports the presence of georeferencing, if a GeoTIFF file is cloud optimized, etc. (Logical)
#' @param report_failures Report failures if file type is unidentified (Logical)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_dataset_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_dataset_identify.
#' # See GDAL documentation at:
#' https://gdal.org/en/release-3.13/programs/gdal_dataset_identify.html
#' job <- gdal_dataset_identify()
#' # gdal_job_run(job)
#' }
#' @export
gdal_dataset_identify <- function(filename,
  output = NULL,
  quiet = FALSE,
  output_format = NULL,
  creation_option = NULL,
  layer_creation_option = NULL,
  output_layer = NULL,
  overwrite = FALSE,
  recursive = FALSE,
  force_recursive = FALSE,
  detailed = FALSE,
  report_failures = FALSE,
  ...) {
  new_args <- list()
  if (!missing(filename)) new_args[["filename"]] <- filename
  if (!missing(output)) new_args[["output"]] <- output
  if (!missing(quiet)) new_args[["quiet"]] <- quiet
  if (!missing(output_format)) new_args[["output_format"]] <- output_format
  if (!missing(creation_option)) new_args[["creation_option"]] <- creation_option
  if (!missing(layer_creation_option)) new_args[["layer_creation_option"]] <- layer_creation_option
  if (!missing(output_layer)) new_args[["output_layer"]] <- output_layer
  if (!missing(overwrite)) new_args[["overwrite"]] <- overwrite
  if (!missing(recursive)) new_args[["recursive"]] <- recursive
  if (!missing(force_recursive)) new_args[["force_recursive"]] <- force_recursive
  if (!missing(detailed)) new_args[["detailed"]] <- detailed
  if (!missing(report_failures)) new_args[["report_failures"]] <- report_failures


  if (!missing(filename) && inherits(filename, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- filename
    new_args[["filename"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("dataset", "identify"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_dataset_identify", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    filename = list(min_count = 0, max_count = 2147483647),
    output = list(min_count = 0, max_count = 1),
    quiet = list(min_count = 0, max_count = 1),
    output_format = list(min_count = 0, max_count = 1),
    creation_option = list(min_count = 0, max_count = 2147483647),
    layer_creation_option = list(min_count = 0, max_count = 2147483647),
    output_layer = list(min_count = 0, max_count = 1),
    overwrite = list(min_count = 0, max_count = 1),
    recursive = list(min_count = 0, max_count = 1),
    force_recursive = list(min_count = 0, max_count = 1),
    detailed = list(min_count = 0, max_count = 1),
    report_failures = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("dataset", "identify"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

