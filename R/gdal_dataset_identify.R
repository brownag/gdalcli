# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.12.0
# Generation date: 2025-12-14
# ===================================================================

#' @title identify: Identify driver opening dataset(s)
#' @description
#' Identify driver opening dataset(s).
#' 
#' See \url{https://gdal.org/en/3.12.0/programs/gdal_dataset_identify.html} for detailed GDAL documentation.
#' @param filename File or directory name (Character vector) (required). `0` to `2147483647` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param output_format Output format. Choices: json, text
#' @param recursive Recursively scan files/folders for datasets (Logical)
#' @param force_recursive Recursively scan folders for datasets, forcing recursion in folders recognized as valid formats (Logical)
#' @param report_failures Report failures if file type is unidentified (Logical)
#' @return A [gdal_job] object.
#' @family gdal_dataset_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_dataset_identify.
#' # See GDAL documentation: https://gdal.org/programs/gdal-dataset-identify.html
#' job <- gdal_dataset_identify()
#' # gdal_job_run(job)
#' }
#' @export
gdal_dataset_identify <- function(filename,
  output_format = NULL,
  recursive = FALSE,
  force_recursive = FALSE,
  report_failures = FALSE) {
  new_args <- list()
  if (!missing(filename)) new_args[["filename"]] <- filename
  if (!missing(output_format)) new_args[["output_format"]] <- output_format
  if (!missing(recursive)) new_args[["recursive"]] <- recursive
  if (!missing(force_recursive)) new_args[["force_recursive"]] <- force_recursive
  if (!missing(report_failures)) new_args[["report_failures"]] <- report_failures

  # Check if first argument is a piped gdal_job or actual data
  if (!missing(filename) && inherits(filename, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- filename
    new_args[["filename"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("dataset", "identify"), new_args))
  }

  # First argument is actual data or missing - create new job
  merged_args <- new_args

  .arg_mapping <- list(
    filename = list(min_count = 0, max_count = 2147483647),
    output_format = list(min_count = 0, max_count = 1),
    recursive = list(min_count = 0, max_count = 1),
    force_recursive = list(min_count = 0, max_count = 1),
    report_failures = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("dataset", "identify"), arguments = merged_args, arg_mapping = .arg_mapping)
}

