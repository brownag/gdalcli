# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.3
# ===================================================================

#' @title reproject: Reproject a raster dataset
#' @description
#' `gdal raster reproject` can be used to reproject a raster dataset. The program can reproject to any supported projection. First, gdal raster reproject must determine the extent and resolution of the output, if these have not been specified using --bbox and --resolution. These are determined by transforming a sample of points from the source CRS to the destination CRS. Details of the procedure can be found in the documentation for :cppGDALSuggestedWarpOutput. If multiple inputs are provided to gdal raster reproject, the output extent will be calculated to cover all of them, at a resolution consistent with the highest-resolution input. Once the dimensions of the output image have been determined, gdal raster reproject divides the output image into chunks that can be processed independently. gdal raster reproject then iterates over scanlines in these chunks, and for each output pixel determines a rectangular region of source pixels that contribute to the value of the output pixel. The dimensions of this rectangular region are typically determined by estimating the relative scales of the source and destination raster, but can be manually specified (see documentation of the ``XSCALE`` parameter in :cppGDALWarpOptions::papszWarpOptions). Because the source region is a simple rectangle, it is not possible for an output pixel to be associated with source pixels from both sides of the antimeridian or pole (when transforming from geographic coordinates). The rectangular region of source pixels is then provided to a function that performs the resampling algorithm selected with --resampling. Depending on the resampling algorithm and relative scales of the source and destination rasters, source pixels may be weighted either according to the approximate fraction of the source pixel that is covered by the destination pixel (e.g., "mean" and "sum" resampling), or by horizontal and vertical Cartesian distances between the center of the source pixel and the center of the target pixel (e.g., bilinear or cubic spline resampling). In the latter case, the relative weight of an individual source pixel is determined by the product of the weights determined for its row and column; the diagonal Cartesian distance is not calculated. This subcommand is also available as a potential step of gdal_raster_pipeline
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_raster_reproject.html} for detailed GDAL documentation.
#' @param input Input raster datasets (required). Exactly `1` value(s). Can also be a [gdal_job] object to extend a pipeline
#' @param input_format Input formats (Character vector) (Advanced)
#' @param input_crs Input CRS
#' @param input_nodata Set nodata values for input bands ('None' to unset). (Character vector). `1` to `2147483647` value(s) (Advanced)
#' @param output Output raster dataset (Dataset path) (required)
#' @param output_format Output format ("GDALG" allowed)
#' @param output_crs Output CRS
#' @param target_aligned_pixels Round target extent to target resolution (Logical) (Advanced)
#' @param output_nodata Set nodata values for output bands ('None' to unset). (Character vector). `1` to `2147483647` value(s) (Advanced)
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>` (Advanced)
#' @param quiet Quiet mode (no progress bar or warning message) (Logical) (Common)
#' @param creation_option Creation option (Character vector). Format: `<KEY>=<VALUE>`
#' @param overwrite Whether overwriting existing output dataset is allowed (Logical) (Default: `false`)
#' @param append Append as a subdataset to existing output (Logical) (Default: `false`)
#' @param like Dataset to use as a template for target bounds, CRS, size and nodata (Dataset path). Format: `DATASET`
#' @param resampling Resampling method. Choices: "nearest", "bilinear", "cubic", "cubicspline", "lanczos", ... (Default: `nearest`)
#' @param resolution Target resolution (in destination CRS units). Format: `<xres>,<yres>`. Minimum: `0`. Exactly `2` value(s)
#' @param size Target size in pixels (Integer vector). Format: `<width>,<height>`. Minimum: `0`. Exactly `2` value(s)
#' @param bbox Target bounding box (in destination CRS units). Exactly `4` value(s)
#' @param bbox_crs CRS of target bounding box
#' @param add_alpha Adds an alpha mask band to the destination when the source raster have none. (Logical) (Advanced)
#' @param warp_option Warping option(s) (Character vector). Format: `<NAME>=<VALUE>` (Advanced)
#' @param transform_option Transform option(s) (Character vector). Format: `<NAME>=<VALUE>` (Advanced)
#' @param error_threshold Error threshold. Minimum: `0` (Advanced)
#' @param num_threads Number of jobs (or ALL_CPUS) (Default: `ALL_CPUS`)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_raster_utilities
#' @examples

#' job <- gdal_raster_reproject(input = "in.tif", output = "out.tif", overwrite = TRUE, 
#'     output_crs = "EPSG:32632")
#' \dontrun{
#'   result <- gdal_job_run(job)
#' }
#' @export
gdal_raster_reproject <- function(input,
  input_format = NULL,
  input_crs = NULL,
  input_nodata = NULL,
  output,
  output_format = NULL,
  output_crs = NULL,
  target_aligned_pixels = FALSE,
  output_nodata = NULL,
  open_option = NULL,
  quiet = FALSE,
  creation_option = NULL,
  overwrite = FALSE,
  append = FALSE,
  like = NULL,
  resampling = NULL,
  resolution = NULL,
  size = NULL,
  bbox = NULL,
  bbox_crs = NULL,
  add_alpha = FALSE,
  warp_option = NULL,
  transform_option = NULL,
  error_threshold = NULL,
  num_threads = NULL,
  ...) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(input_format)) new_args[["input_format"]] <- input_format
  if (!missing(input_crs)) new_args[["input_crs"]] <- input_crs
  if (!missing(input_nodata)) new_args[["input_nodata"]] <- input_nodata
  if (!missing(output)) new_args[["output"]] <- output
  if (!missing(output_format)) new_args[["output_format"]] <- output_format
  if (!missing(output_crs)) new_args[["output_crs"]] <- output_crs
  if (!missing(target_aligned_pixels)) new_args[["target_aligned_pixels"]] <- target_aligned_pixels
  if (!missing(output_nodata)) new_args[["output_nodata"]] <- output_nodata
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(quiet)) new_args[["quiet"]] <- quiet
  if (!missing(creation_option)) new_args[["creation_option"]] <- creation_option
  if (!missing(overwrite)) new_args[["overwrite"]] <- overwrite
  if (!missing(append)) new_args[["append"]] <- append
  if (!missing(like)) new_args[["like"]] <- like
  if (!missing(resampling)) new_args[["resampling"]] <- resampling
  if (!missing(resolution)) new_args[["resolution"]] <- resolution
  if (!missing(size)) new_args[["size"]] <- size
  if (!missing(bbox)) new_args[["bbox"]] <- bbox
  if (!missing(bbox_crs)) new_args[["bbox_crs"]] <- bbox_crs
  if (!missing(add_alpha)) new_args[["add_alpha"]] <- add_alpha
  if (!missing(warp_option)) new_args[["warp_option"]] <- warp_option
  if (!missing(transform_option)) new_args[["transform_option"]] <- transform_option
  if (!missing(error_threshold)) new_args[["error_threshold"]] <- error_threshold
  if (!missing(num_threads)) new_args[["num_threads"]] <- num_threads


  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("raster", "reproject"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_raster_reproject", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    input = list(min_count = 1, max_count = 1),
    input_format = list(min_count = 0, max_count = 2147483647),
    input_crs = list(min_count = 0, max_count = 1),
    input_nodata = list(min_count = 1, max_count = 2147483647),
    output = list(min_count = 0, max_count = 1),
    output_format = list(min_count = 0, max_count = 1),
    output_crs = list(min_count = 0, max_count = 1),
    target_aligned_pixels = list(min_count = 0, max_count = 1),
    output_nodata = list(min_count = 1, max_count = 2147483647),
    open_option = list(min_count = 0, max_count = 2147483647),
    quiet = list(min_count = 0, max_count = 1),
    creation_option = list(min_count = 0, max_count = 2147483647),
    overwrite = list(min_count = 0, max_count = 1),
    append = list(min_count = 0, max_count = 1),
    like = list(min_count = 0, max_count = 1),
    resampling = list(min_count = 0, max_count = 1),
    resolution = list(min_count = 2, max_count = 2),
    size = list(min_count = 2, max_count = 2),
    bbox = list(min_count = 4, max_count = 4),
    bbox_crs = list(min_count = 0, max_count = 1),
    add_alpha = list(min_count = 0, max_count = 1),
    warp_option = list(min_count = 0, max_count = 2147483647),
    transform_option = list(min_count = 0, max_count = 2147483647),
    error_threshold = list(min_count = 0, max_count = 1),
    num_threads = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("raster", "reproject"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

