# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.3
# ===================================================================

#' @title check: Check whether there are errors when reading the content of a dataset
#' @description
#' Check whether there are errors when reading the content of a dataset.
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_dataset_check.html} for detailed GDAL documentation.
#' @param input Input raster, vector or multidimensional raster dataset (Dataset path) (required). Can also be a [gdal_job] object to extend a pipeline
#' @param quiet Quiet mode (no progress bar or warning message) (Logical) (Common)
#' @param open_option Open options (Character vector). Format: `<KEY>=<VALUE>` (Advanced)
#' @param input_format Input formats (Character vector) (Advanced)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_dataset_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_dataset_check.
#' # See GDAL documentation at:
#' https://gdal.org/en/release-3.13/programs/gdal_dataset_check.html
#' job <- gdal_dataset_check()
#' # gdal_job_run(job)
#' }
#' @export
gdal_dataset_check <- function(input,
  quiet = FALSE,
  open_option = NULL,
  input_format = NULL,
  ...) {
  new_args <- list()
  if (!missing(input)) new_args[["input"]] <- input
  if (!missing(quiet)) new_args[["quiet"]] <- quiet
  if (!missing(open_option)) new_args[["open_option"]] <- open_option
  if (!missing(input_format)) new_args[["input_format"]] <- input_format


  if (!missing(input) && inherits(input, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- input
    new_args[["input"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("dataset", "check"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_dataset_check", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    input = list(min_count = 0, max_count = 1),
    quiet = list(min_count = 0, max_count = 1),
    open_option = list(min_count = 0, max_count = 2147483647),
    input_format = list(min_count = 0, max_count = 2147483647)
  )

  new_gdal_job(command_path = c("dataset", "check"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

