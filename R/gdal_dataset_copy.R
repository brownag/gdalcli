# ===================================================================
# This file is AUTO-GENERATED by build/generate_gdal_api.R
# Do not edit directly. Changes will be overwritten on regeneration.
# Generated for GDAL 3.13.0
# ===================================================================

#' @title copy: Copy files of a dataset
#' @description
#' Copy files of a dataset
#' 
#' See \url{https://gdal.org/en/release-3.13/programs/gdal_dataset_copy.html} for detailed GDAL documentation.
#' @param source Source dataset name (required). Can also be a [gdal_job] object to extend a pipeline
#' @param destination Destination dataset name (required)
#' @param overwrite Whether overwriting existing output dataset is allowed (Logical) (Default: `false`)
#' @param format Dataset format (Advanced)
#' @param ... Parameter aliases and synonyms for backward compatibility. See `?gdal_parameter_aliases` for details.
#' @return A [gdal_job] object.
#' @family gdal_dataset_utilities
#' @examples
#' \dontrun{
#' # TODO: No examples available for gdal_dataset_copy.
#' # See GDAL documentation at:
#' https://gdal.org/en/release-3.13/programs/gdal_dataset_copy.html
#' job <- gdal_dataset_copy()
#' # gdal_job_run(job)
#' }
#' @export
gdal_dataset_copy <- function(source,
  destination,
  overwrite = FALSE,
  format = NULL,
  ...) {
  new_args <- list()
  if (!missing(source)) new_args[["source"]] <- source
  if (!missing(destination)) new_args[["destination"]] <- destination
  if (!missing(overwrite)) new_args[["overwrite"]] <- overwrite
  if (!missing(format)) new_args[["format"]] <- format


  if (!missing(source) && inherits(source, 'gdal_job')) {
    # First argument is a piped job - extend the pipeline
    # Remove first_arg from new_args since it's the job, not data
    piped_job <- source
    new_args[["source"]] <- NULL
    new_args <- Filter(Negate(is.null), new_args)
    return(extend_gdal_pipeline(piped_job, c("dataset", "copy"), new_args))
  }


  merged_args <- .merge_alias_parameters(list(...), new_args)
  .update_intent_mapping <- NULL
  .update_intent <- infer_update_intent("gdal_dataset_copy", merged_args, .update_intent_mapping)

  .arg_mapping <- list(
    source = list(min_count = 0, max_count = 1),
    destination = list(min_count = 0, max_count = 1),
    overwrite = list(min_count = 0, max_count = 1),
    format = list(min_count = 0, max_count = 1)
  )

  new_gdal_job(command_path = c("dataset", "copy"), arguments = merged_args, arg_mapping = .arg_mapping, update_intent = .update_intent)
}

